#include <iostream>
#include <vector>
#include <array>
#include <algorithm>


using namespace std;


template<typename T>
T maxVal(T a, T b) {
    return a > b ? a:b;
}

int main(int argc, const char* argv[]) {
    return 0;
}